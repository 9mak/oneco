# ============================================================================
# このファイルは pyproject.toml と同期されています。
# 信頼できる単一源泉は pyproject.toml [project] dependencies です。
# requirements.txt は Docker / GitHub Actions の互換のため維持されています。
# 依存を変更するときは両方を同時に更新してください。
# Codex リリースレビュー I-5 で指摘された不整合への対応。
# ============================================================================

# Core dependencies (pyproject.toml と同期)
pydantic>=2.5.0
beautifulsoup4>=4.12.0
requests>=2.31.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.19.0
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic-settings>=2.0.0
alembic>=1.11.0
anthropic>=0.40.0
openai>=1.0.0
pyyaml>=6.0
playwright>=1.40.0
pdfplumber>=0.10.0
python-dotenv>=1.0.0
feedgen>=1.0.0
redis[asyncio]>=5.0.0
slowapi>=0.1.9

# Testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
httpx>=0.25.0
# 上限を固定: メジャー更新による current-event-loop 挙動変更で
# 「ローカル通過・CI失敗」が起きたため (1.4.0 で顕在化)。
pytest-asyncio>=0.21.0,<2.0
numpy>=1.24.0
