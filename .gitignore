# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
!frontend/lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
.venv/
env/
ENV/
env.bak/
venv.bak/

# PyCharm
.idea/

# VS Code
.vscode/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/

# Data Collector Runtime
.collector.lock
snapshots/*
!snapshots/latest.json
output/*
!output/animals.json

# Environment variables
.env
.env.local
.env.*.local
.envrc

# macOS
.DS_Store

# Logs
*.log

# 移行用 venv（コミット対象外）
.venv-mig/
.claude/scheduled_tasks.lock

# Playwright MCP のページスナップショット（自動生成、コミット対象外）
.playwright-mcp/

# rule-based migration: local secrets loader & PoC artifacts
.envrc
scripts/poc_kochi_selectors.yaml
scripts/poc_selectors/
AGENTS.md
release-screenshot-*.png
test-*.png

# 開発中スクリーンショット (PR や Discord 共有用に local 撮影、コミット対象外)
oneco-*.png
# ブランドアセットは例外 (SNSアバター等の公式画像)
!frontend/public/brand/oneco-*.png
mobile-header.png

# ローカル SQLite (本番は Supabase、local 専用 cache)
oneco_local.db

# .mcp.json: MCP server local config (project-scoped 個人設定、共有しない)
.mcp.json

# node_modules: frontend/ 配下は frontend/.gitignore で除外済みだが、root で
# `npm install` した場合に root/node_modules ができる (Vercel build や誤実行)。
node_modules/
