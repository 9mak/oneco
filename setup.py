# ============================================================================
# 互換 shim: 全ての依存・メタデータは pyproject.toml で管理。
# このファイルは `pip install -e .` の旧バックエンド互換のためだけに存在し、
# install_requires / extras_require は意図的に列挙しない（pyproject.toml と
# 二重管理になり不整合の原因になるため）。
# Codex リリースレビュー I-5 で指摘された依存関係の分散・不整合の対応。
# ============================================================================
from setuptools import setup

setup()
